1.
I'm building a Flutter workout tracking app called "Martial Body".

Before anything else, read these three files:

1. `/home/robinroy/Documents/Personal Projects/Martial Body/docs/architecture.md`
2. `/home/robinroy/Documents/Personal Projects/Martial Body/docs/PRD.md`
3. `/home/robinroy/Documents/Personal Projects/Martial Body/docs/Phase 1 - Training.docx`

To read the .docx file run:
cd "/home/robinroy/Documents/Personal Projects/Martial Body/docs" && pip install python-docx -q && python3 -c "import docx; doc=docx.Document('Phase 1 - Training.docx'); [print(p.text) for p in doc.paragraphs]"

After reading all three, respond with:
1. A summary of the Phase 1 workout structure (how many weeks, days per week, exercises per day, what data each exercise has)
2. A summary of the PRD goals
3. A summary of the architecture

Do NOT write any code yet. Wait for my confirmation that your understanding is correct.





2.
Now that we've confirmed the understanding, do the following:

1. Initialize a Flutter project inside `/home/robinroy/Documents/Personal Projects/Martial Body/` if one doesn't exist yet (app name: martial_body)
2. Set up the folder structure exactly as described in architecture.md
3. Define the data models only — no UI yet. Models should cover: Phase, Week, Day, Exercise, WorkoutLog, Set
4. Hardcode Phase 1 workout data from the .docx into a Dart data file (not a database yet — just structured Dart code)

After finishing, show me:
- The folder structure you created
- The data models
- A sample of the hardcoded Phase 1 data (first week, first day)

Do NOT build any UI yet. Wait for my confirmation.





3.
Now build the app shell with navigation and all screens, but no business logic yet. Just the structure and layout.

Screens needed:
- Home screen (shows current week and today's workout)
- Workout detail screen (shows exercises for the day)
- Log set screen (where I'll enter reps/weight per set)
- History screen (list of past logged workouts)

Use the navigation structure from architecture.md. Placeholder data is fine for now.

After finishing, build and run the app:
flutter run

Show me any errors. Do NOT add workout logging logic yet. Wait for my confirmation.





4.Now add the core functionality: logging workouts.

Requirements:
- I can open today's workout and see all exercises
- For each exercise I can log sets: reps done + weight used
- I can mark a set as complete
- I can mark the full day as complete
- Data must persist locally (use the storage method from architecture.md)

After finishing:
- Run the app and confirm it builds
- Walk me through exactly how to log one exercise so I can test it myself

Wait for my confirmation before any further changes.






5.
The logging works and I've tested it. Now do a final polish pass:

1. Fix any UI rough edges (spacing, alignment, readability)
2. Make sure the home screen correctly shows today's workout based on the current date and Phase 1 start date (my start date is tomorrow: [INSERT DATE])
3. Add basic error handling — app should not crash on empty states
4. Build a release APK:

flutter build apk --release

Tell me exactly where the APK file is saved so I can sideload it to my phone.